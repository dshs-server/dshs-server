import LedgerPrototype from "@/design-lab/ledger/LedgerPrototype";

export const metadata = {
  title: "DSHS GPU실 — 디자인 시안 03",
  description: "컬러 배경 위 White Liquid Glass UI 프로토타입",
};

export default function WhiteGlassDesignPage() {
  return <LedgerPrototype variant="white-glass" />;
}
