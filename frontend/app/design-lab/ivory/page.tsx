import LedgerPrototype from "@/design-lab/ledger/LedgerPrototype";

export const metadata = {
  title: "DSHS GPU실 — 디자인 시안 04",
  description: "화이트 아틀리에 UI 프로토타입",
};

export default function IvoryDesignPage() {
  return <LedgerPrototype variant="ivory" />;
}
