import LedgerPrototype from "@/design-lab/ledger/LedgerPrototype";

export const metadata = {
  title: "DSHS GPU실 — 디자인 시안 02",
  description: "Liquid Glass 작업 원장 UI 프로토타입",
};

export default function LedgerDesignPage() {
  return <LedgerPrototype />;
}
