import PrototypeApp from "@/design-lab/PrototypeApp";

export const metadata = {
  title: "DSHS Compute — Design Lab",
  description: "로그인 이후 화면을 위한 Liquid Glass UI 프로토타입",
};

export default function DesignLabPage() {
  return <PrototypeApp />;
}
